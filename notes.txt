#ts-components-event-props
main 2 events [click,change]

click event : you have to set ButtonProps like...

type ButtonProps = {
     'handle_click':(event:React.MouseEvent<HTMLButtonElement>,id:string)=> void
}

change event : you have to set inputProps like...

type InputBox = {
    'handle_click':(event:React.ChangeEvent<HTMLInputElement>)=> void
}

##need to do a task related to events geting value from the input box.